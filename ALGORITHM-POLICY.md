# Cubestow 알고리즘 정책과 한계

- 문서 버전: 3.4
- 기준 서비스: Cubestow 1.1.8 공개 베타
- 갱신일: 2026-09-24

이 문서는 Cubestow가 적재안을 만드는 기준, 결과를 해석하는 방법과 현재 계산하지 못하는 영역을 공개한다. 새로운 알고리즘을 추가할 때도 이 문서의 안전 원칙과 변경 절차를 따른다.

## 1. 결과의 성격

Cubestow의 결과는 컨테이너 적재 계획을 빠르게 검토하기 위한 휴리스틱 제안이다. 전역 최적해, 구조 안전 인증 또는 현장 상차 지시를 의미하지 않는다. 실제 적재 전에는 화물 사양, 컨테이너 상태, 운송 조건과 고정재 제조사 지침을 현장 책임자가 확인해야 한다.

## 2. 현재 계산 흐름

1. 직접 입력과 Excel/CSV 데이터를 하나의 제품 모델로 정규화한다.
2. 부피와 중량으로 필요한 컨테이너 대수의 하한을 계산한다.
3. 제품별 허용 회전 방향을 생성한다. 바닥면 회전은 평가하고, 높이 축 변경은 `눕힘 허용` 제품에만 적용한다.
4. 컨테이너 바닥과 이미 배치한 화물의 끝점, 그리고 그 점을 반대 축으로 투영한 점을 Extreme Point 후보로 만든다.
5. 후보마다 화물을 아래 → 안쪽 → 좌측으로 밀어 다른 화물이나 벽에 닿게 한다. 떠 있는 위치는 만들지 않는다.
6. 후보마다 경계, 충돌, 중량, 안전 기준(지지율·세장 화물), 상부 적재 금지, 중심 지지, 측면 지지, 상부 허용하중과 스택 합성 무게중심을 검사한다.
7. 유효 후보는 배치 규칙별 키를 **항목 순서대로** 비교한다. 운송모드별 위험이 있는 후보는 모든 배치 규칙에서 위험이 없는 후보보다 뒤로 간다.
8. 배치 규칙 4종(바닥 우선 DBLF, 공간 집약, 폭 채움, 무게중심)과 투입 순서 4종(부피, 최장변, 바닥면, 중량)으로 16개 배치안을 만든다. 투입 순서가 같아 결과가 같을 수밖에 없는 배치안은 한 번만 계산한다.
9. 컨테이너마다 적재 부피 → 적재 수량 → 우선 기준 순으로 한 안을 고른다. 화물은 컨테이너 안쪽 벽에 붙여 싣고, 좌우로만 질량중심을 맞춘 안을 함께 비교한다. 안쪽 벽에 붙였을 때 전후 무게중심 편차가 CTU 사전검사의 위험 수준(10% 초과)이면, 편차가 권고 범위(5%)에 들어오는 만큼만 문 쪽으로 옮기고 결과 설명에 표시한다. 이때 생긴 안쪽 벽 간극은 고정재 검토 대상이다.
10. 남은 화물은 같은 규격의 다음 컨테이너에서 같은 방식으로 계산한다. 폭 조합은 그 컨테이너에 남은 화물로만 다시 계산한다.
11. 결과를 생성 엔진과 분리된 validator로 다시 검사한 뒤 적재 순서와 고정재 권고를 만든다.
12. 이전 결과가 한 대로 완료됐고 컨테이너·안전 기준·우선 기준·운송모드가 같으면, 유효한 기존 배치를 유지하고 바뀐 화물만 다시 배치한다(최소 변경 복구).

같은 입력과 설정에는 같은 결과를 낸다. 단, 계산이 시간 예산(기본 8초)을 넘으면 남은 후보를 생략하고 결과에 `시간 제한으로 일부 후보 생략`을 표시하며, 이때는 기기 성능에 따라 결과가 달라질 수 있다. 계산은 Web Worker에서 실행하고, Worker를 쓸 수 없는 환경에서는 같은 엔진을 화면 스레드에서 실행한다.

현재 알고리즘 식별자는 `ep-lex-portfolio-2026.10.1`이다. 저장 프로젝트에는 이 식별자와 계산 결과 요약을 함께 기록하며, 알고리즘 버전이 바뀌면 결과도 달라질 수 있다.

### 순서 비교 원칙

후보와 결과는 여러 항목을 가중치로 더하지 않고 앞 항목부터 차례로 비교한다. 이전 방식(항목마다 10의 거듭제곱을 곱해 합산)은 부동소수점 유효숫자(약 16자리) 때문에 뒤 항목이 무시되는 문제가 있었다.

무게중심은 배치된 화물의 기하학적 중심에 개당 중량을 곱한 정적 모멘트로 계산한다. 컨테이너 중심 대비 전후·좌우 편차가 모두 5% 이하면 `안정`, 최대 편차가 10% 이하면 `주의`, 그보다 크면 `위험`으로 표시한다. 이 판정은 축하중, 바닥 집중하중, 마찰, 가감속과 운송 중 동하중을 포함하지 않는다.

## 3. 공통 하드 조건

다음 조건을 통과하지 못한 후보는 점수가 좋아도 배치하지 않는다.

- 컨테이너 내부 경계를 넘지 않음
- 다른 화물과 3차원 영역이 겹치지 않음
- 컨테이너 최대 적재 중량을 넘지 않음
- 상부 적재 금지 화물 위에 다른 화물을 놓지 않음
- 상부 화물의 중심이 실제 지지 영역 안에 있음
- 원통 위 박스는 배치하지 않음(박스 바닥이 원통 윗면에 일부라도 걸리면 제외)
- 원통 위 원통은 중심 오차 15mm 이하, 지름 차이 합계 30mm 이하
- 입력 수량은 적재 수량과 미배치 수량의 합과 일치
- 상부 스택의 합성 무게중심이 지지영역 안에 있음
- 제품별 상부 허용하중이 입력된 경우, 접촉면적 비율로 전달한 누적 상부하중이 허용값을 넘지 않음

## 4. 안전 기준과 우선 기준

사용자는 안전 기준 하나와 우선 기준 하나를 고른다. 안전 기준은 하드 조건이고, 우선 기준은 같은 안전 기준 안에서 **미배치 수량과 컨테이너 대수가 같은 결과** 중 하나를 고르는 소프트 기준이다. 우선 기준 때문에 컨테이너가 늘어나지 않는다. 엄격 기준에서 표준 기준의 결과를 대신 쓰지 않는다.

### 안전 기준

| 기준 | 상부 지지율 | 상부 화물 세장비(높이 ÷ 바닥 최소 치수) |
|---|---|---|
| 엄격 (기본) | 100% | 1.15 이하 |
| 표준 | 70% 이상 | 제한 없음 |

다음 조건은 두 기준에 공통이다.

- 누적 높이가 바닥 최소 치수의 1.5배를 넘는 화물은 바닥 화물을 포함해 최소 2방향 측면 지지를 요구한다.
- 상부 화물 중심이 실제 지지 영역 안에 있어야 한다.

### 우선 기준

| 기준 | 비교 순서 (미배치·대수가 같을 때) |
|---|---|
| 자동 추천 (기본) | CTU 사전검사 등급 → 운송 안정성 검토 건수 → 60-in-50·수직 CoG 초과량 → 최대 중심 편차 → 사용 길이 |
| 공간 활용 | 사용 길이 → CTU 등급 → 검토 건수 → 중심 편차 |
| 폭 균형 | 운송 안정성 검토 건수 → CTU 등급 → 사용 길이 → 중심 편차 |
| 무게중심 | CTU 등급 → 최대 중심 편차 → 검토 건수 → 사용 길이 |

### 배치 규칙 (내부 생성기)

| 규칙 | 바닥 먼저 | 후보 비교 순서 |
|---|---|---|
| 바닥 우선 DBLF | 예 | 운송 위험 → 높이 → 안쪽 → 좌측 → 위험도 → 내부 틈 → 조합 잔여 폭 |
| 공간 집약 | 아니오 | 운송 위험 → 사용 길이 → 내부 틈 → 높이 → 조합 잔여 폭 |
| 폭 채움 | 예 | 운송 위험 → 조합 잔여 폭 → 내부 틈 → 현재 빈 폭 → 미지지 방향 수 → 높이 → 사용 길이 |
| 무게중심 | 아니오 | 운송 위험 → 사용 길이 → 내부 틈 → 높이 → 질량중심 편차 |

배치 규칙은 우선 기준과 무관하게 모두 실행한다. 폭이 꽉 찬 모양이나 중앙 배치는 안정성의 보조 신호일 뿐이며, 실제 래싱·마찰·운송 중 동하중 검증을 대신하지 않는다. 중앙 배치로 생기는 문측·안쪽·좌우 간극은 고정재 검토 대상이다.

### 이전 전략과의 대응

저장 스키마 4 이하의 프로젝트는 불러올 때 다음과 같이 옮긴다.

| 이전 전략 | 안전 기준 | 우선 기준 |
|---|---|---|
| 자동 추천, 안전 우선 | 엄격 | 자동 추천 |
| 안전·폭 균형 | 엄격 | 폭 균형 |
| 공간 활용 우선 | 표준 | 공간 활용 |
| 무게중심 우선 | 엄격 | 무게중심 |

이전 자동 추천은 표준(70%) 지지 결과도 후보에 넣었다. 새 자동 추천은 선택한 안전 기준 안에서만 고른다.

## 5. 운송모드별 안정성과 고정재

- 전·후·좌·우 각 방향에서 화물 면의 폭 또는 높이가 50% 이상 맞닿을 때 기하학적 지지로 인정한다. 이 접촉률은 CTU Code의 고정력이나 관성력 50%를 의미하지 않는다.
- 해상운송은 피칭·롤링·히빙에 불리한 높은 적층, 측면 지지 부족, 문측 상단 노출과 원통 구름 위험을 가장 보수적으로 선별한다.
- 복합운송은 해상과 육상 구간을 함께 고려하는 기본값이며, 육상운송은 높이와 측면 지지 임계값을 일부 완화한다.
- 모든 배치 규칙에서 운송모드별 위험이 없는 위치를 먼저 고르고, 위험 후보끼리는 높이 비율·적층 높이·미지지 방향 수가 작은 위치를 우선한다. 따라서 운송모드는 사후 경고 문구뿐 아니라 실제 배치 위치에도 영향을 준다.
- 자동 래싱 밴드 수량과 3D 경로는 생성하지 않는다. 위험 화물은 원인에 따라 `현장 고정 검토` 또는 `배치 재검토`로 표시한다.
- 문쪽 노출 화물에는 가로 각재와 부목을 우선 권고한다.
- 벽면 간극 100mm 이상 또는 화물 사이 간극 120~600mm에는 충돌이 없을 때 에어백을 권고한다.
- 이 권고는 실제 고정재 강도, 에어백 압력이나 바닥 고정 허용 여부를 계산한 설계가 아니다.

### CTU Code 반영 정책

- CTU Code를 컨테이너 적입·고정 안전 검토의 기준 프레임워크로 사용한다.
- 현재 버전은 결합 무게중심 편차, 수직 무게중심과 `총질량 60%가 컨테이너 길이 50%에 집중되는지`를 사전검사한다.
- 상부 허용하중이 입력된 화물은 상부 화물 중량을 접촉면적 비율로 아래 화물에 전달하여 누적 압축하중을 검증한다. 미입력 화물은 안전하다고 가정하지 않고 `미검증`으로 표시한다.
- 운송모드별 미끄럼·전도를 참고 계산한다. 가속도 계수는 IMO MSC.1/Circ.1498 Informative Material 5의 Quick Lashing Guide A·B·C 표(도로 좌우 0.5g·전방 0.8g·후방 0.5g, 해상 A/B/C 좌우 0.5/0.7/0.8g·전후 0.3/0.3/0.4g와 각 수직 계수)를 쓴다. 해상은 해역을 모르면 가장 가혹한 C, 복합운송은 도로와 해상 C 중 불리한 값을 쓴다.
- 벽 또는 벽까지 이어진 화물에 면의 50% 이상 닿은 방향만 막힌 것으로 본다. 문은 충격하중 방지 조건을 확인할 수 없으므로 막힌 경계로 보지 않는다.
- 막히지 않은 방향마다 미끄럼 억제력 m·g·(c − μ·v)의 합계와 전도 위험 화물 수를 보고한다. 마찰계수는 재질 조합을 확인하지 못했을 때의 최대 허용값 0.3을 쓴다. 전도는 위에 얹힌 화물을 포함한 적층 높이로 H/B ≤ v/(c·n)을 검사하며, n은 같은 줄에서 서로 기대는 화물 수다. 이 식은 같은 문서 전도 표의 열 수별 경계와 일치하고, 전후 방향에는 보수적으로 같은 식을 적용한다.
- 이 계산은 배치 결과를 바꾸지 않는 검토 정보다. 무게중심은 화물 중앙으로 가정하며 LC/MSL, STF, 래싱 수량과 벽·앵커 허용하중은 계산하지 않는다.
- 해당 입력과 방향별 힘·모멘트 검증이 구현되기 전에는 `CTU Code 준수` 또는 `래싱 안전 충족`으로 표시하지 않는다.

## 6. 성공·부분 적재·실패 판정

| 상태 | 기준 |
|---|---|
| 적재 완료 | 입력 화물이 모두 배치됨 |
| 부분 적재 | 일부는 배치됐지만 미배치 화물이 남음 |
| 적재 불가 | 배치된 화물이 없고 미배치 화물이 존재함 |

계산 진행률 100%는 계산 작업이 끝났다는 뜻이며 적재 성공률과 다르다. 미배치 화물이 있으면 Excel 내보내기를 허용하지 않는다.

## 7. 현재 계산하지 않는 항목

- 제품별 압축강도, 최대 상부 하중과 접촉 압력
- 바닥 집중하중과 차량 축하중
- 화물별 실제 마찰계수, 충격·진동과 운송 중 이동량
- 래싱 용량, 밴드 각도·수량, 에어백 압력과 파열 한계
- 팔레트 강도, 포장 변형과 원통의 실제 곡면 접촉
- 위험물 격리, 냉동 컨테이너 공기 순환과 법규 적합성
- 컨테이너 실측 오차, 돌출물, 문 개구 치수와 작업 장비 공간
- 모든 배치 조합을 탐색하는 전역 최적화

## 8. 알고리즘 추가·변경 정책

새 알고리즘이나 점수 규칙은 다음 조건을 만족해야 배포한다.

1. 공통 하드 조건을 우회하지 않는다.
2. 기존 전략과 같은 입력으로 비교 가능한 결과를 만든다.
3. 적재 수량뿐 아니라 경계·충돌·중량·지지 불변식 테스트를 통과한다.
4. 실패와 미배치 이유를 사용자에게 숨기지 않는다.
5. 결과가 크게 달라지는 변경은 이 문서의 버전과 변경 이력에 기록한다.
6. 실험 전략은 기본 전략과 구분하고 사용자가 선택 여부를 알 수 있게 한다.
7. 독립 validator, 기준 benchmark와 단일 시작 대비 회귀 게이트를 통과한다.
8. 학습 기반 선택기는 하드 안전조건을 변경하지 않고 검증된 전략의 실행 순서만 선택한다.

## 9. 변경 이력

| 문서 버전 | 날짜 | 주요 내용 |
|---|---|---|
| 1.0 | 2026-08-11 | Extreme Point·DBLF, 안전/최대/자동 전략, 측면 지지·래싱과 현재 한계 정리 |
| 1.1 | 2026-08-11 | 저장 스키마 v2, 알고리즘 식별자와 결과 요약 보존 정책 추가 |
| 1.2 | 2026-08-11 | 짧은 방향 잔여 폭을 우선 최소화하는 폭 우선 적재 전략 추가 |
| 1.3 | 2026-08-11 | 저장 스키마 v3, 무게중심 판정과 현장 결과 비교 정책 추가 |
| 1.4 | 2026-08-13 | 5개 전략, 결과 기반 자동 추천, 무게중심 우선과 CTU Code 반영 범위 추가 |
| 1.5-next | 2026-08-13 | 독립 validator, 합성 CoG 지지 검사, EP 자유후보 제거, 조건부 결정론적 multi-start와 benchmark gate 추가 |
| 1.8 | 2026-08-14 | 자동 밴드 제거, 해상·복합·육상 운송모드별 안정성 검토 추가 |
| 1.9 | 2026-08-14 | 운송모드별 안정성 위험을 후보 배치 점수에 직접 반영하고 단일 컨테이너 안내 문구 수정 |
| 2.0 | 2026-08-15 | 무게중심 전략도 정식 Extreme Point를 사용하고 완성 화물군의 질량중심 기반 경계 내 이동, 미배치 시 안전 우선 포트폴리오 폴백 추가 |
| 2.1 | 2026-08-15 | 입력 화물 기반 폭 조합, 전략별 안전·공간 폴백, 중복 안정성 검토 그룹화, 10개 현장 시나리오와 전 컨테이너 규격 품질 게이트 추가 |
| 2.2 | 2026-08-15 | 10개 시나리오에서 적재 대수·미배치 결과가 모두 같음을 확인하고, 별도 폭 최적화를 더 안전한 안전·폭 균형 전략으로 통합 |
| 2.3 | 2026-08-15 | 엄격한 전략이 공간 활용 우선의 부분 지지 결과를 대체안으로 채택하지 않도록 전략 경계를 고정하고 혼합 화물 회귀검사 추가 |
| 2.4 | 2026-08-15 | 안전 우선·안전폭 균형 재계산 시 기존의 유효한 배치를 유지하고 변경 조건을 충족하지 못한 화물만 재배치하는 최소 변경 복구 추가 |
| 3.0 | 2026-09-24 | 전략 5종을 안전 기준(엄격·표준) × 우선 기준(자동·공간·폭·무게중심)으로 재구성, 같은 안전 기준에서는 미배치·대수를 먼저 최소화, 가중합 점수를 항목별 순서 비교로 교체, Extreme Point 투영·밀착 후보, 배치 규칙 4종 × 투입 순서 4종 포트폴리오, 부피·중량 하한 표시, 시간 예산, Web Worker 분리, 엔진 식별자 `ep-lex-portfolio-2026.09` |
| 3.1 | 2026-09-24 | 두 번째 이후 컨테이너의 폭 조합을 남은 화물로 다시 계산(앞 컨테이너에 모두 실린 규격의 폭을 가정하던 결함 수정, 200개 혼합 화물 엄격 기준 3대 → 2대), 결과가 같은 포트폴리오 실행 생략, 상부 허용하중 증분 검사(판정 동일), 엔진 식별자 `ep-lex-portfolio-2026.10` |
| 3.2 | 2026-09-24 | CTU Code 참고 계산 추가: IMO Quick Lashing Guide 가속도 계수로 막히지 않은 방향의 미끄럼 억제력과 적층·열 수를 고려한 전도 위험을 결과 화면·PDF·Excel에 표시(배치 결과는 변경 없음) |
| 3.3 | 2026-09-24 | 화물을 안쪽 벽에 붙여 싣도록 변경(길이 방향 무게중심 이동 제거). 붙였을 때 전후 편차가 위험 수준이면 권고 범위까지만 문 쪽으로 옮기는 예외 처리, 엔진 식별자 `ep-lex-portfolio-2026.10.1` |
| 3.4 | 2026-09-24 | 스택 합성 무게중심 조건을 validator뿐 아니라 배치 단계에서도 검사(긴 판재를 엇갈려 쌓을 때 독립 검증에 실패하던 결함 수정). 상부하중·합성 무게중심 누적값을 커밋마다 증분 갱신 |
