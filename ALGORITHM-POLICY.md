# Cubestow 알고리즘 정책과 한계

- 문서 버전: 3.23
- 기준 서비스: Cubestow 1.1.36 공개 베타
- 갱신일: 2026-09-24

이 문서는 Cubestow가 적재안을 만드는 기준, 결과를 해석하는 방법과 현재 계산하지 못하는 영역을 공개한다. 새로운 알고리즘을 추가할 때도 이 문서의 안전 원칙과 변경 절차를 따른다.

## 1. 결과의 성격

Cubestow의 결과는 컨테이너 적재 계획을 빠르게 검토하기 위한 휴리스틱 제안이다. 전역 최적해, 구조 안전 인증 또는 현장 상차 지시를 의미하지 않는다. 실제 적재 전에는 화물 사양, 컨테이너 상태, 운송 조건과 고정재 제조사 지침을 현장 책임자가 확인해야 한다.

## 2. 현재 계산 흐름

1. 직접 입력과 Excel/CSV 데이터를 하나의 제품 모델로 정규화한다.
2. 부피와 중량으로 필요한 컨테이너 대수의 하한을 계산한다.
3. 제품별 허용 회전 방향을 생성한다. 바닥면 회전은 평가하고, 높이 축 변경은 `눕힘 허용` 제품에만 적용한다.
4. 컨테이너 바닥과 이미 배치한 화물의 끝점, 그리고 그 점을 반대 축으로 투영한 점을 Extreme Point 후보로 만든다.
5. 후보마다 화물을 아래 → 안쪽 → 좌측으로 밀어 다른 화물이나 벽에 닿게 한다. 떠 있는 위치는 만들지 않는다.
6. 후보마다 경계, 충돌, 중량, 안전 기준(지지율·세장 화물), 상부 적재 금지, 중심 지지, 측면 지지, 상부 허용하중과 스택 합성 무게중심을 검사한다.
7. 유효 후보는 배치 규칙별 키를 **항목 순서대로** 비교한다. 운송모드별 위험이 있는 후보는 모든 배치 규칙에서 위험이 없는 후보보다 뒤로 간다.
8. 배치 규칙 6종(같은 규격 기둥 쌓기, 균형 기둥 쌓기, 바닥 우선 DBLF, 공간 집약, 폭 채움, 무게중심)과 투입 순서 4종(부피, 최장변, 바닥면, 중량)으로 24개 배치안을 만든다. 자동 추천은 두 기둥 쌓기를 가장 먼저 계산해 시간 예산 안에 반드시 포함한다. 남은 화물을 모두 실었고 CTU 사전검사가 양호한 배치안이 나오면 더 나은 안이 있을 수 없으므로 나머지 계산을 멈춘다. 투입 순서가 같아 결과가 같을 수밖에 없는 배치안은 한 번만 계산한다.
9. 컨테이너마다 적재 부피 → 적재 수량 → 우선 기준 순으로 한 안을 고른다. 자동 추천에서 안전 등급·편차 구간·운송 검토 수가 같으면 큰 화물(부피 제곱 가중)이 안쪽 벽 쪽에 있는 안을 고른다(실무 관행: 큰 화물을 먼저 안쪽에). 적재 전체를 앞뒤로 뒤집은 안도 후보로 비교한다. 화물은 컨테이너 안쪽 벽에 붙여 싣고, 좌우로만 질량중심을 맞춘 안을 함께 비교한다. 첫 적재는 예외 없이 안쪽 벽에 붙이며 길이 방향으로 옮기지 않는다. 배치안마다 어떤 화물도 걸치지 않는 길이 방향 절단면으로 적재를 슬라이스로 나누고, 슬라이스 순서, 좌우 반전, 그리고 슬라이스 구간 안의 앞뒤 반전(거울상)을 바꿔 무게중심을 가운데로 맞춘 안을 함께 비교한다(Davies & Bischoff 1999의 벽 교환·반전). 적재가 한 덩어리여서 절단면이 없어도 앞뒤 반전으로 무거운 부분을 가운데 쪽으로 옮길 수 있다. 재배열한 안은 적재 순서상 첫 화물(바닥 화물)이 안쪽 벽에 붙어 있을 때만 쓴다. 받침·상부하중·적층 무게중심은 슬라이스 안에서만 생기므로 그대로이고, 앞뒤 이웃이 바뀌는 측면 지지 규칙은 다시 검사한다. 첫 슬라이스는 항상 안쪽 벽부터 놓는다. 남는 전후 무게 쏠림은 CTU 사전검사의 주의·위험 표시로 알린다.
10. 계산량은 경과 시간이 아니라 문제 크기로 정한다: 컨테이너마다 배치안 1회 비용을 화물 수의 제곱으로 어림하고(엄격·많이 싣기 1.6×10⁻⁵ n²초, CTU 안전 9×10⁻⁵ n²초), 예산(기본 8초 ÷ 필요 대수 하한) 안에 드는 배치안 수만큼 계산한다(최소 4회, CTU 안전에서 1회가 매우 무거우면 2회). 같은 입력이면 컴퓨터 속도와 관계없이 같은 결과가 나온다. 시간은 비상 상한(예산의 3배와 45초 중 큰 값)만 본다.
11. 남은 화물은 같은 규격의 다음 컨테이너에서 같은 방식으로 계산한다. 폭 조합은 그 컨테이너에 남은 화물로만 다시 계산한다.
12. 결과를 생성 엔진과 분리된 validator로 다시 검사한 뒤 적재 순서와 고정재 권고를 만든다.
13. 이전 결과가 한 대로 완료됐고 컨테이너·안전 기준·우선 기준·운송모드가 같으면, 유효한 기존 배치를 유지하고 바뀐 화물만 다시 배치한다(최소 변경 복구).

같은 입력과 설정에는 같은 결과를 낸다. 단, 계산이 시간 예산(기본 8초)을 넘으면 남은 후보를 생략하고 결과에 `시간 제한으로 일부 후보 생략`을 표시하며, 이때는 기기 성능에 따라 결과가 달라질 수 있다. 계산은 Web Worker에서 실행하고, Worker를 쓸 수 없는 환경에서는 같은 엔진을 화면 스레드에서 실행한다.

현재 알고리즘 식별자는 `ep-lex-portfolio-2026.10.17`이다. 저장 프로젝트에는 이 식별자와 계산 결과 요약을 함께 기록하며, 알고리즘 버전이 바뀌면 결과도 달라질 수 있다.

### 순서 비교 원칙

후보와 결과는 여러 항목을 가중치로 더하지 않고 앞 항목부터 차례로 비교한다. 이전 방식(항목마다 10의 거듭제곱을 곱해 합산)은 부동소수점 유효숫자(약 16자리) 때문에 뒤 항목이 무시되는 문제가 있었다.

무게중심은 배치된 화물의 기하학적 중심에 개당 중량을 곱한 정적 모멘트로 계산한다. 컨테이너 중심 대비 전후·좌우 편차가 모두 5% 이하면 `안정`, 최대 편차가 10% 이하면 `주의`, 그보다 크면 `위험`으로 표시한다. 이 판정은 축하중, 바닥 집중하중, 마찰, 가감속과 운송 중 동하중을 포함하지 않는다.

## 3. 공통 하드 조건

다음 조건을 통과하지 못한 후보는 점수가 좋아도 배치하지 않는다.

- 컨테이너 내부 경계를 넘지 않음
- 다른 화물과 3차원 영역이 겹치지 않음
- 컨테이너 최대 적재 중량을 넘지 않음
- 상부 적재 금지 화물 위에 다른 화물을 놓지 않음
- 상부 화물의 중심이 실제 지지 영역 안에 있음
- 원통 위 박스는 배치하지 않음(박스 바닥이 원통 윗면에 일부라도 걸리면 제외)
- 원통 위 원통은 중심 오차 15mm 이하, 지름 차이 합계 30mm 이하
- 엄격 기준에서 원통은 바닥이나 원통 위에만 세운다. 상자 위에 올린 원통(예: 펌프 위 케이블 드럼 2단)은 높은 곳에서 곡면이 벽에 선으로만 닿아 기울거나 구를 위험이 크다. validator도 엄격 기준에서 같은 규칙을 검사한다.
- 입력 수량은 적재 수량과 미배치 수량의 합과 일치
- 상부 스택의 합성 무게중심이 지지영역 안에 있음
- 제품별 상부 허용하중이 입력된 경우, 접촉면적 비율로 전달한 누적 상부하중이 허용값을 넘지 않음

## 4. 안전 기준과 우선 기준

사용자는 안전 기준 하나와 우선 기준 하나를 고른다. 안전 기준은 하드 조건이고, 우선 기준은 같은 안전 기준 안에서 **미배치 수량과 컨테이너 대수가 같은 결과** 중 하나를 고르는 소프트 기준이다. 우선 기준 때문에 컨테이너가 늘어나지 않는다. 엄격 기준에서 표준 기준의 결과를 대신 쓰지 않는다.

### 안전 기준

| 기준 | 상부 지지율 | 상부 화물 세장비(높이 ÷ 바닥 최소 치수) | 추가 조건 |
|---|---|---|---|
| CTU 안전 | 100% | 1.15 이하 | 엄격 조건 + 모든 화물의 안쪽·좌·우 3면 막힘. 래싱을 쓰지 않으면 CTU 전도 기준(운송모드별 H/B ≤ v/c)을 모든 화물에 적용 |
| 엄격 (기본) | 100% | 1.15 이하 | 원통은 바닥이나 원통 위에만, 높은 적층 전도 방지(한계 3) |
| 많이 싣기 | 70% 이상 | 제한 없음 | 적재량 우선. 충돌·지지·상부하중·상부적재금지 같은 물리 조건만 지킨다 |

CTU 안전 기준의 "막힘"은 다음 중 하나다. 문쪽 면은 각재·부목·스트랩으로 막으므로 예외다.

- 안쪽 면: 안쪽 벽에 닿거나, 600mm 이내 뒤쪽 화물들의 면이 그 면 면적의 50% 이상을 덮는다(합집합, 에어백으로 채울 간극 포함).
- 좌·우 면: 600mm 이내 옆 화물들의 면이 50% 이상을 덮거나, 벽까지 사이에 화물이 없어 벽 간극을 막을 수 있다. 바닥 화물은 간극이 커도 충전재(세운 빈 팔레트·골판지)와 에어백으로 막을 수 있고, 높은 곳 화물은 에어백 한계인 600mm 이하 간극만 인정한다.
- 간극 한계는 에어백 제조사 최대 간극 500mm(Stopak 1500×2400, CTU §2.3.8은 제조사 한계를 따르게 함)다. 높은 곳 화물의 벽 간극도 500mm 이하만 인정한다.
- CTU 안전은 전폭 벽(스트립) 빌더를 함께 계산한다: 같은 규격을 같은 방향으로 수직으로 쌓은 기둥들(규격·회전·단수)로 폭 2,352mm를 채우는 벽을 만들되, 벽 안의 기둥 높이를 맞추고(목표 높이와의 차이가 한 단의 절반 이하), 기둥 높이는 전도 한계 안에서만 쌓는다. (깊이, 높이)마다 폭 조합을 동적계획법으로 골라 벽 부피를 최대로 하고 양 끝 잔여 폭은 500mm 이하를 우선하며, 채움률이 가장 높은 벽을 안쪽부터 차례로 확정한다. 또한 폭 전체를 채우는 층(벽)을 안쪽 벽부터 쌓는 벽 쌓기(George & Robinson 1980) 배치안을 함께 계산한다. 층마다 후보 깊이로 앞 층을 고정한 채 임시로 채우고 최종 규칙으로 검사한 뒤, 채움률이 가장 높은 층을 고른다. 최종 검사에서 빠진 화물은 다시 임시로 놓고 검사하기를 여러 번 반복해 통과한 안 중 부피가 가장 큰 안을 쓴다. 아직 아무것도 싣지 못한 컨테이너는 시간 예산이 지나도 다음 배치안을 계속 시도한다.
- 얹힘 금지: 바닥면 크기가 다른 화물 위에 따로 올린 화물은 문쪽 면이 막혀 있어야 한다(앞에 같은 높이의 화물이 있거나 문쪽 첫 줄). 같은 규격을 줄 맞춰 쌓은 기둥의 윗단은 얹힘이 아니다. validator도 같은 규칙을 검사한다.
- 고정재 선택: 사용자가 에어백·충전재·바닥 못·래싱을 켜고 끌 수 있다. 에어백과 충전재를 모두 끄면 틈을 채울 수 없으므로 직접 닿은 면만 막힘으로 본다. 충전재를 끄면 바닥 화물의 넓은 벽 간극도 에어백 한계 안에서만 인정한다.
- 래싱을 끄면 "막거나 묶거나"(CTU) 중 묶는 방법이 없으므로, CTU 정보자료 5 가속도의 전도 한계(복합 = 도로·해상 C 중 불리한 값: 좌우 1.25, 앞뒤 0.5)를 넘는 방향은 모두 막혀야 한다. 문쪽 첫 줄은 문쪽 고정(각재·펜스)으로 막는 것으로 본다. 최종 검사에서 문쪽 면만 모자라 걸린 화물은 앞을 어중간하게 막는 화물을 빼서 다시 놓는다.
- 배치 중에는 1면 이상이면 임시로 두고, 완성 후 미달 화물을 빼서 다시 놓는다. 배치 단계에서는 이미 막힌 자리를 먼저 고른다. validator도 최종 상태에서 같은 규칙을 독립 검사한다.

엄격 이상의 높은 적층 전도 방지: 쌓인 화물(z>0)의 (바닥부터 높이 ÷ 그 방향 폭)이 3을 넘으면, 그 방향 양쪽 면(길이 방향이면 문쪽·안쪽, 폭 방향이면 좌·우)이 위 "막힘" 기준으로 막혀 있어야 한다. 문쪽 면은 앞에 아무 화물도 없으면(문쪽 첫 줄) 도어 펜스·스트랩으로 막는 것으로 본다. 한계 3은 도로 운송 전도 한계(수직 1g ÷ 문쪽·좌우 0.5g = 2)보다 여유를 둔 값이며, 앞줄이 낮아 윗단 앞면이 비는 계단식 적층(예: 소형 박스 5단 1.6m)을 걸러낸다. 이 규칙은 최종 적재 상태에서 판정하고 validator도 같은 규칙으로 검사한다.

다음 조건은 모든 기준에 공통이다.

- 누적 높이가 바닥 최소 치수의 1.5배를 넘는 화물은 바닥 화물을 포함해 최소 2방향 측면 지지를 요구한다. 한 방향은 벽이거나, 그 면 폭의 50% 이상 맞닿은 화물들이 면 높이의 50% 이상을 덮을 때 지지된 것으로 본다. 높이는 맞닿은 화물들의 높이 구간 합집합으로 계산하므로, 박스를 쌓은 기둥도 한 개의 큰 화물처럼 벽 역할을 한다(기둥의 각 박스는 자체 지지·상부하중·적층 무게중심 검사를 이미 통과한 상태다). 이 규칙은 **최종 적재 상태**에서 판정한다. 배치 중에는 1면 이상 닿으면 임시로 놓고(옆 칸이 나중에 채워지는 경우), 컨테이너를 다 채운 뒤 2면 미만인 화물과 그 위 화물을 빼서 배치 시점 2면 규칙으로 다시 놓는다. 좌우 무게중심 맞춤·슬라이스 재배열을 거친 완성안도 모든 화물이 2면 이상인지 확인하고, 아니면 버린다. 독립 validator도 같은 규칙을 최종 상태에서 검사한다.
- 상부 화물 중심이 실제 지지 영역 안에 있어야 한다.

### 우선 기준

| 기준 | 비교 순서 (미배치·대수가 같을 때) |
|---|---|
| 자동 추천 (기본) | CTU 사전검사 등급 → 운송 안정성 검토 건수 → 60-in-50·수직 CoG 초과량 → 최대 중심 편차 → 사용 길이 |
| 공간 활용 | 사용 길이 → CTU 등급 → 검토 건수 → 중심 편차 |
| 폭 균형 | 운송 안정성 검토 건수 → CTU 등급 → 사용 길이 → 중심 편차 |
| 무게중심 | CTU 등급 → 최대 중심 편차 → 검토 건수 → 사용 길이 |

### 배치 규칙 (내부 생성기)

| 규칙 | 바닥 먼저 | 후보 비교 순서 |
|---|---|---|
| 균형 기둥 쌓기 | 예 | 기둥 쌓기와 같되, 제품 그룹이 차지할 길이로 무게중심이 가운데에 가장 가까운 그룹 순서를 고르고, 최대 높이로 쌓았을 때 바닥이 남으면 그만큼 기둥을 낮춰 길이 방향으로 펼침 |
| 같은 규격 기둥 쌓기 | 예 | 같은 제품을 같은 방향으로 수직 기둥처럼 쌓아 안쪽 벽부터 세운 뒤 나머지를 바닥 우선으로 채움. 기둥 높이는 컨테이너 높이·상부 허용하중·상부 적재 금지·세장비로 제한 |
| 바닥 우선 DBLF | 예 | 운송 위험 → 높이 → 안쪽 → 좌측 → 위험도 → 내부 틈 → 조합 잔여 폭 |
| 공간 집약 | 아니오 | 운송 위험 → 사용 길이 → 내부 틈 → 높이 → 조합 잔여 폭 |
| 폭 채움 | 예 | 운송 위험 → 조합 잔여 폭 → 내부 틈 → 현재 빈 폭 → 미지지 방향 수 → 높이 → 사용 길이 |
| 무게중심 | 아니오 | 운송 위험 → 사용 길이 → 내부 틈 → 높이 → 질량중심 편차 |

배치 규칙은 우선 기준과 무관하게 모두 실행한다. 폭이 꽉 찬 모양이나 중앙 배치는 안정성의 보조 신호일 뿐이며, 실제 래싱·마찰·운송 중 동하중 검증을 대신하지 않는다. 중앙 배치로 생기는 문측·안쪽·좌우 간극은 고정재 검토 대상이다.

### 이전 전략과의 대응

저장 스키마 4 이하의 프로젝트는 불러올 때 다음과 같이 옮긴다.

| 이전 전략 | 안전 기준 | 우선 기준 |
|---|---|---|
| 자동 추천, 안전 우선 | 엄격 | 자동 추천 |
| 안전·폭 균형 | 엄격 | 폭 균형 |
| 공간 활용 우선 | 표준 | 공간 활용 |
| 무게중심 우선 | 엄격 | 무게중심 |

이전 자동 추천은 표준(70%) 지지 결과도 후보에 넣었다. 새 자동 추천은 선택한 안전 기준 안에서만 고른다.

## 5. 운송모드별 안정성과 고정재

### 기준 출처 원칙

모든 적재·고정 기준은 **CTU Code(IMO/ILO/UNECE 화물운송단위 적입 실무규칙, MSC.1/Circ.1497)와 부속서 7, 정보자료 5(빠른 래싱 가이드, MSC.1/Circ.1498)**를 기본으로 한다. CTU가 수치를 정하지 않은 곳은 CTU가 따르라고 한 제조사 기준을 쓰고, 그래도 없으면 Cubestow 설정값으로 표시한다.

| 항목 | 값 | 출처 |
|---|---|---|
| 방향별 빈 공간 합 | 15cm 이하, 넘으면 채움 | CTU 부속서 7 §2.3.6 |
| 문 경계 | 충격하중이 없을 때만 경계로 인정, 문쪽에 에어백 금지 | CTU 부속서 7 §4.2.5, §2.3.8 |
| 에어백 최대 간극 | 500mm(1500×2400), 규격별 백 폭의 약 1/3 | CTU §2.3.8 → 제조사(Stopak) |
| 에어백 설치 | 바닥에 닿지 않게, 화물 높이 이내, 넘는 간극은 충전재로 줄임 | 제조사(Cordstrap) |
| 못 고정 | 못 1개 1~4kN, 하한 1kN으로 못 수 계산, 바닥 두께 2/3 이상 | CTU 부속서 7, FM 55-80 |
| 가속도·마찰 | 도로·해상 A/B/C 가속도 표, 마찰계수 0.3 | CTU 정보자료 5 |
| 스트랩 사전장력 | MSL의 50% 이하 | CTU 부속서 7 §4.1.4 |
| 각재 펜스 | 50×100mm 각재, 뒤 기둥 고정 | CTU 부록 계산 예 |
| 무게중심·60/50 | 편심 ±5%(±10%), 길이 50%에 60% 이하 | CTU 3장 |
| 에어백 최소 간극 50mm, 바닥 띄움 100mm, 스페이서/에어백 경계 120mm, 10mm 미만 틈 제외 | — | Cubestow 설정 |
| 막힘 판정(면의 50% 이상 덮임), 측면 지지 2면(높이 비율 1.5), 높은 적층 한계 3(엄격·래싱 사용) | — | Cubestow 설정 |
| 전도 한계(CTU 안전 + 래싱 끔) | 운송모드별 H/B ≤ v/c | CTU 정보자료 5 |


- 전·후·좌·우 각 방향에서 화물 면의 폭 또는 높이가 50% 이상 맞닿을 때 기하학적 지지로 인정한다. 이 접촉률은 CTU Code의 고정력이나 관성력 50%를 의미하지 않는다.
- 해상운송은 피칭·롤링·히빙에 불리한 높은 적층, 측면 지지 부족, 문측 상단 노출과 원통 구름 위험을 가장 보수적으로 선별한다.
- 복합운송은 해상과 육상 구간을 함께 고려하는 기본값이며, 육상운송은 높이와 측면 지지 임계값을 일부 완화한다.
- 모든 배치 규칙에서 운송모드별 위험이 없는 위치를 먼저 고르고, 위험 후보끼리는 높이 비율·적층 높이·미지지 방향 수가 작은 위치를 우선한다. 따라서 운송모드는 사후 경고 문구뿐 아니라 실제 배치 위치에도 영향을 준다.
- 자동 래싱 밴드 수량과 3D 경로는 생성하지 않는다. 위험 화물은 원인에 따라 `현장 고정 검토` 또는 `배치 재검토`로 표시한다.
- 문쪽 노출 화물에는 가로 각재와 부목을 우선 권고한다.
- 벽면 간극 50mm 이상 또는 화물 사이 간극 120~600mm에는 충돌이 없을 때 에어백을 권고한다. 에어백은 다른 에어백·부목·화물과 겹치지 않는 위치에만 둔다. 컨테이너 끝(안쪽 벽·문 쪽)에는 에어백을 두지 않는다(CTU Code 부속서 7 §2.3.8: 문쪽 공간을 백으로 채우지 않는다).
- 에어백 한 개가 막는 간극은 600mm 이하다. 제조사 권장 최대 간극은 백 폭의 약 1/3이다(예: Stopak 600×1200 200mm, 900×1800 300mm, 1200×1800 400mm, 1500×2400 500mm, Cordstrap 최대 약 700mm). 간극에 맞춰 규격을 표시한다(≤200 600×1200, ≤300 900×1800, ≤400 1200×1800, 그 이상 1500×2400).
- 600mm를 넘는 벽 간극은 백을 키우거나 여러 개를 이어 붙이지 않고, 백과 벽 사이를 충전재(세운 빈 팔레트·골판지)로 채운다(CTU Code 부속서 7 §2.3.6, Cordstrap 핸드북).
- 에어백은 컨테이너 바닥에 닿지 않게 100mm 띄워 설치하고, 화물 높이를 넘지 않는다.
- 문쪽 줄에 윗단 화물이 있으면 상단을 도어 스트랩(웹 래싱)으로 측면·바닥 고정점에 묶도록 권고한다(문은 충격하중이 없을 때만 경계로 본다, CTU Code 부속서 7 §4.2.5). 스트랩 사전장력은 MSL의 50% 이하(§4.1.4). 문쪽 간극에는 가로 각재와 지지 부목을 두고, 간극이 크면 충전재를 더한다.
- 3D 보기와 권고 목록에 고정재 아이콘(에어백·각재·스트랩·충전재)을 표시한다.
- 문쪽: 앞에 화물이 없는 문쪽 화물이 문에서 150mm 넘게 떨어져 있으면, 바닥 화물은 문쪽 면 바로 앞 바닥에 가로 각재를 대고 못으로 고정한다(틈이 크면 쐐기 부목 추가). 못 수는 (화물 + 그 위 적층 질량) × g × 문쪽 필요 가속도(운송모드별 CTU 가속도 c − 마찰 0.3 × v) ÷ 1kN이다. 윗단 노출 화물은 뒤 기둥 사이 가로 각재 펜스와 충전재로 막는다. 150mm 이하의 틈은 문을 경계로 본다. CTU Code는 밀폐형 컨테이너 바닥 못질을 일반적으로 권장하지 않으므로, 못질은 컨테이너 운영자(선사) 허용을 확인해야 한다.
- 남은 틈: 화물마다 네 옆면에서 가장 가까운 화물·벽·고정재까지의 틈을 본다. 그 면을 지나는 선의 빈 공간 합이 15cm를 넘을 때만(CTU §2.3.6) 큰 틈부터 채운다. 120mm 미만은 스페이서(골판지·목재), 120~500mm는 에어백(높은 곳이면 그 높이에 설치), 안쪽 벽 쪽 틈은 에어백 대신 충전재. 높은 곳 화물의 틈이 에어백 최대 간극(500mm)을 넘거나 문쪽이 비면 화물 위로 넘기는 상단 래싱(좌우 측벽 고정점, 사전장력 MSL의 50% 이하)을 권고한다. 에어백은 컨테이너당 최대 40개까지 표시한다.
- 이 권고는 실제 고정재 강도, 에어백 압력이나 바닥 고정 허용 여부를 계산한 설계가 아니다.

### CTU Code 반영 정책

- CTU Code를 컨테이너 적입·고정 안전 검토의 기준 프레임워크로 사용한다.
- 현재 버전은 결합 무게중심 편차, 수직 무게중심과 `총질량 60%가 컨테이너 길이 50%에 집중되는지`를 사전검사한다.
- 중심 편차(양호 ±5% · 주의 ±10% · 위험 초과)와 60-in-50은 스프레더·차량이 실제로 드는 총중량(화물 + 컨테이너 자체중량) 기준으로 판정한다. 자체중량은 컨테이너 가운데에 있다고 보고, 명시값이 없으면 ISO 건화물 대표값(20ft 2,230 · 40ft 3,750 · 40ft HC 3,940 · 45ft HC 4,800 kg)을 쓴다. 무거운 적재는 화물 기준과 거의 같고, 가벼운 부분 적재는 편차가 자체중량 비율만큼 줄어든다(차량 하중분포도로 허용 범위를 정하는 Ramos·Silva·Oliveira 2018의 방식을 단순화). 수직 무게중심은 화물만으로 판정한다. 화면에는 화물만의 편차도 함께 보여 준다.
- 상부 허용하중이 입력된 화물은 상부 화물 중량을 접촉면적 비율로 아래 화물에 전달하여 누적 압축하중을 검증한다. 미입력 화물은 안전하다고 가정하지 않고 `미검증`으로 표시한다.
- 운송모드별 미끄럼·전도를 참고 계산한다. 가속도 계수는 IMO MSC.1/Circ.1498 Informative Material 5의 Quick Lashing Guide A·B·C 표(도로 좌우 0.5g·전방 0.8g·후방 0.5g, 해상 A/B/C 좌우 0.5/0.7/0.8g·전후 0.3/0.3/0.4g와 각 수직 계수)를 쓴다. 해상은 해역을 모르면 가장 가혹한 C, 복합운송은 도로와 해상 C 중 불리한 값을 쓴다.
- 벽 또는 벽까지 이어진 화물에 면의 50% 이상 닿은 방향만 막힌 것으로 본다. 문은 충격하중 방지 조건을 확인할 수 없으므로 막힌 경계로 보지 않는다.
- 막히지 않은 방향마다 미끄럼 억제력 m·g·(c − μ·v)의 합계와 전도 위험 화물 수를 보고한다. 마찰계수는 재질 조합을 확인하지 못했을 때의 최대 허용값 0.3을 쓴다. 전도는 위에 얹힌 화물을 포함한 적층 높이로 H/B ≤ v/(c·n)을 검사하며, n은 같은 줄에서 서로 기대는 화물 수다. 이 식은 같은 문서 전도 표의 열 수별 경계와 일치하고, 전후 방향에는 보수적으로 같은 식을 적용한다.
- 이 계산은 배치 결과를 바꾸지 않는 검토 정보다. 무게중심은 화물 중앙으로 가정하며 LC/MSL, STF, 래싱 수량과 벽·앵커 허용하중은 계산하지 않는다.
- 해당 입력과 방향별 힘·모멘트 검증이 구현되기 전에는 `CTU Code 준수` 또는 `래싱 안전 충족`으로 표시하지 않는다.

## 6. 성공·부분 적재·실패 판정

| 상태 | 기준 |
|---|---|
| 적재 완료 | 입력 화물이 모두 배치됨 |
| 부분 적재 | 일부는 배치됐지만 미배치 화물이 남음 |
| 적재 불가 | 배치된 화물이 없고 미배치 화물이 존재함 |

계산 진행률 100%는 계산 작업이 끝났다는 뜻이며 적재 성공률과 다르다. 미배치 화물이 있으면 Excel 내보내기를 허용하지 않는다.

## 7. 현재 계산하지 않는 항목

- 제품별 압축강도, 최대 상부 하중과 접촉 압력
- 바닥 집중하중과 차량 축하중
- 화물별 실제 마찰계수, 충격·진동과 운송 중 이동량
- 래싱 용량, 밴드 각도·수량, 에어백 압력과 파열 한계
- 팔레트 강도, 포장 변형과 원통의 실제 곡면 접촉
- 위험물 격리, 냉동 컨테이너 공기 순환과 법규 적합성
- 컨테이너 실측 오차, 돌출물, 문 개구 치수와 작업 장비 공간
- 모든 배치 조합을 탐색하는 전역 최적화

## 8. 알고리즘 추가·변경 정책

새 알고리즘이나 점수 규칙은 다음 조건을 만족해야 배포한다.

1. 공통 하드 조건을 우회하지 않는다.
2. 기존 전략과 같은 입력으로 비교 가능한 결과를 만든다.
3. 적재 수량뿐 아니라 경계·충돌·중량·지지 불변식 테스트를 통과한다.
4. 실패와 미배치 이유를 사용자에게 숨기지 않는다.
5. 결과가 크게 달라지는 변경은 이 문서의 버전과 변경 이력에 기록한다.
6. 실험 전략은 기본 전략과 구분하고 사용자가 선택 여부를 알 수 있게 한다.
7. 독립 validator, 기준 benchmark와 단일 시작 대비 회귀 게이트를 통과한다.
8. 학습 기반 선택기는 하드 안전조건을 변경하지 않고 검증된 전략의 실행 순서만 선택한다.

## 9. 변경 이력

| 문서 버전 | 날짜 | 주요 내용 |
|---|---|---|
| 1.0 | 2026-08-11 | Extreme Point·DBLF, 안전/최대/자동 전략, 측면 지지·래싱과 현재 한계 정리 |
| 1.1 | 2026-08-11 | 저장 스키마 v2, 알고리즘 식별자와 결과 요약 보존 정책 추가 |
| 1.2 | 2026-08-11 | 짧은 방향 잔여 폭을 우선 최소화하는 폭 우선 적재 전략 추가 |
| 1.3 | 2026-08-11 | 저장 스키마 v3, 무게중심 판정과 현장 결과 비교 정책 추가 |
| 1.4 | 2026-08-13 | 5개 전략, 결과 기반 자동 추천, 무게중심 우선과 CTU Code 반영 범위 추가 |
| 1.5-next | 2026-08-13 | 독립 validator, 합성 CoG 지지 검사, EP 자유후보 제거, 조건부 결정론적 multi-start와 benchmark gate 추가 |
| 1.8 | 2026-08-14 | 자동 밴드 제거, 해상·복합·육상 운송모드별 안정성 검토 추가 |
| 1.9 | 2026-08-14 | 운송모드별 안정성 위험을 후보 배치 점수에 직접 반영하고 단일 컨테이너 안내 문구 수정 |
| 2.0 | 2026-08-15 | 무게중심 전략도 정식 Extreme Point를 사용하고 완성 화물군의 질량중심 기반 경계 내 이동, 미배치 시 안전 우선 포트폴리오 폴백 추가 |
| 2.1 | 2026-08-15 | 입력 화물 기반 폭 조합, 전략별 안전·공간 폴백, 중복 안정성 검토 그룹화, 10개 현장 시나리오와 전 컨테이너 규격 품질 게이트 추가 |
| 2.2 | 2026-08-15 | 10개 시나리오에서 적재 대수·미배치 결과가 모두 같음을 확인하고, 별도 폭 최적화를 더 안전한 안전·폭 균형 전략으로 통합 |
| 2.3 | 2026-08-15 | 엄격한 전략이 공간 활용 우선의 부분 지지 결과를 대체안으로 채택하지 않도록 전략 경계를 고정하고 혼합 화물 회귀검사 추가 |
| 2.4 | 2026-08-15 | 안전 우선·안전폭 균형 재계산 시 기존의 유효한 배치를 유지하고 변경 조건을 충족하지 못한 화물만 재배치하는 최소 변경 복구 추가 |
| 3.0 | 2026-09-24 | 전략 5종을 안전 기준(엄격·표준) × 우선 기준(자동·공간·폭·무게중심)으로 재구성, 같은 안전 기준에서는 미배치·대수를 먼저 최소화, 가중합 점수를 항목별 순서 비교로 교체, Extreme Point 투영·밀착 후보, 배치 규칙 4종 × 투입 순서 4종 포트폴리오, 부피·중량 하한 표시, 시간 예산, Web Worker 분리, 엔진 식별자 `ep-lex-portfolio-2026.09` |
| 3.1 | 2026-09-24 | 두 번째 이후 컨테이너의 폭 조합을 남은 화물로 다시 계산(앞 컨테이너에 모두 실린 규격의 폭을 가정하던 결함 수정, 200개 혼합 화물 엄격 기준 3대 → 2대), 결과가 같은 포트폴리오 실행 생략, 상부 허용하중 증분 검사(판정 동일), 엔진 식별자 `ep-lex-portfolio-2026.10` |
| 3.2 | 2026-09-24 | CTU Code 참고 계산 추가: IMO Quick Lashing Guide 가속도 계수로 막히지 않은 방향의 미끄럼 억제력과 적층·열 수를 고려한 전도 위험을 결과 화면·PDF·Excel에 표시(배치 결과는 변경 없음) |
| 3.3 | 2026-09-24 | 화물을 안쪽 벽에 붙여 싣도록 변경(길이 방향 무게중심 이동 제거). 붙였을 때 전후 편차가 위험 수준이면 권고 범위까지만 문 쪽으로 옮기는 예외 처리, 엔진 식별자 `ep-lex-portfolio-2026.10.1` |
| 3.4 | 2026-09-24 | 스택 합성 무게중심 조건을 validator뿐 아니라 배치 단계에서도 검사(긴 판재를 엇갈려 쌓을 때 독립 검증에 실패하던 결함 수정). 상부하중·합성 무게중심 누적값을 커밋마다 증분 갱신 |
| 3.5 | 2026-09-25 | 첫 적재는 예외 없이 안쪽 벽에 밀착(무게 쏠림 시 문 쪽으로 옮기던 예외 제거, 경고로만 표시). 에어백이 서로·부목·화물과 겹치지 않도록 권고 위치를 제한. 엔진 식별자 `ep-lex-portfolio-2026.10.2` |
| 3.6 | 2026-09-25 | 같은 규격 기둥 쌓기 배치 규칙 추가(자동 추천에서 가장 먼저 계산), 단일 규격에서도 회전 폭 조합을 비교(냉장고 36대 2대→1대), 받침 관계 캐시로 안전 검사 가속, 좌우 편차를 앞뒤 편차와 별도 기준으로 비교. 엔진 식별자 `ep-lex-portfolio-2026.10.3` |
| 3.7 | 2026-09-25 | 균형 기둥 쌓기 추가(400개 카톤 앞뒤 편차 16.7%→0.1%), 전부 싣고 CTU 양호인 배치안이 나오면 계산 조기 종료(전체 계산 시간 −36%, 최장 17초→8초). 엔진 식별자 `ep-lex-portfolio-2026.10.4` |
| 3.8 | 2026-09-25 | 슬라이스 재배열 추가: 독립 슬라이스의 순서와 좌우 반전으로 무게중심 보정(평가 세트 74건 CTU 위험 23→15, 운송 검토 항목 111→94, 컨테이너 수·미적재 변화 없음). 부피가 최선보다 작은 배치안은 마무리 계산 생략. 엔진 식별자 `ep-lex-portfolio-2026.10.5` |
| 3.9 | 2026-09-25 | CTU 중심 편차와 60-in-50을 총중량(화물 + 컨테이너 자체중량) 기준으로 판정. 가벼운 부분 적재의 과도한 위험 표시를 줄임(평가 세트 74건 위험 15→8, 주의 22→14, 무거운 부분 적재는 그대로 위험). 배치안 선택 키가 이 등급을 쓰므로 엔진 식별자 `ep-lex-portfolio-2026.10.6` |
| 3.10 | 2026-09-25 | 측면 지지를 맞닿은 화물들의 높이 합집합으로 판단(쌓인 박스 기둥이 장롱·액자 같은 높은 화물을 지지). 균형 기둥 변형은 기둥으로 못 세우는 규격도 정한 그룹 순서대로 바닥에 먼저 놓음. validator의 상부적재금지 검사가 바닥면이 겹치지 않는 같은 높이 화물까지 받침으로 보던 오탐 수정. 가정 이사 화물(엄격) 컨테이너 2대→1대, 평가 세트 74건 컨테이너 87→86, 주의 14→13. 엔진 식별자 `ep-lex-portfolio-2026.10.7` |
| 3.11 | 2026-09-25 | 슬라이스 앞뒤 반전 추가(가정 이사 화물 엄격 기준 총중량 편차 13.5%→3.6%, 위험→주의). 재배열 후 적재 순서상 첫 바닥 화물이 안쪽 벽에 붙는지 확인. 평가 세트 74건 위험 9→8, 주의 13→12, 운송 검토 항목 변화 없음. 엔진 식별자 `ep-lex-portfolio-2026.10.8` |
| 3.12 | 2026-09-25 | 측면 지지 2면 규칙을 최종 적재 상태에서 판정(배치 중 1면 임시 허용 → 완성 후 미달 화물 제거·재배치). 좌우 무게중심 맞춤으로 옆벽에서 떨어진 높은 화물이 2면 미만이 되던 결함 수정(평가 세트 4건). validator에 최종 상태 측면 지지 검사 추가. TV 세움 포장(표준) 컨테이너 2대→1대, 평가 세트 74건 컨테이너 86→85, 주의 12→9. 엔진 식별자 `ep-lex-portfolio-2026.10.9` |
| 3.13 | 2026-09-25 | 엄격 기준: 원통은 바닥이나 원통 위에만(상자 위 원통 금지). 평가 세트 74건 컨테이너 수 변화 없음(85). 고단 화물 3면·4면 지지 규칙도 시험했으나 컨테이너 85→113~118대로 늘어 기본 규칙에는 넣지 않음. 엔진 식별자 `ep-lex-portfolio-2026.10.10` |
| 3.14 | 2026-09-25 | 안전 기준 3단계: 최고 안전(엄격 + 문쪽을 뺀 3면 막힘) 추가. 엄격·표준 결과는 그대로. 평가 세트 엄격 22건 44대 → 최고 안전 52대. 측면 검사 후 재배치를 반복하고, 완성안이 하나도 통과하지 못하면 싣지 않도록 보강. 엔진 식별자 `ep-lex-portfolio-2026.10.11` |
| 3.15 | 2026-09-25 | 고정재 권고: 에어백 1개당 간극 600mm 이하·규격 표시·바닥에서 100mm 띄움, 큰 벽 간극은 충전재 + 에어백, 문쪽 윗단 도어 스트랩, 고정재 아이콘. 배치 결과는 변경 없음 |
| 3.16 | 2026-09-25 | 엄격 이상: 높은 적층 전도 방지(높이/폭 > 3이면 그 방향 양면 막힘), validator 검사 추가(이전 엔진 결과 엄격 37건 중 8건 위반). 같은 안전 등급이면 큰 화물이 안쪽인 안을 선택, 적재 전체 앞뒤 반전 후보 추가. 평가 세트 엄격 44→45대(소형 가전 1→2대), 최고 안전 54→56대, 표준 변화 없음. 엔진 식별자 `ep-lex-portfolio-2026.10.12` |
| 3.17 | 2026-09-25 | 고정재 권고 보강: 문쪽 각재 펜스 + 충전재(바닥 못 부목 제거), 모든 옆면 틈을 크기별로 스페이서·에어백(높은 곳 포함)·충전재로 채움, 높은 곳 큰 틈은 상단 래싱. 샘플 3 에어백 1개 → 에어백 3·펜스 2단·충전재 2·스페이서 9·상단 래싱 4. 배치 결과는 변경 없음 |
| 3.18 | 2026-09-25 | 기준 출처를 CTU Code 기본으로 정리(출처 표 추가). 문쪽 바닥 각재 못 고정과 못 수 계산(1kN/개), 빈 공간 합 15cm 규칙, 에어백 최대 간극 제조사 기준 500mm. 배치 결과는 변경 없음 |
| 3.19 | 2026-09-25 | 안전 기준 이름: 많이 싣기 / 엄격 / CTU 안전. 고정재 선택(에어백·충전재·바닥 못·래싱) 추가: 끈 고정재는 권고에서 다른 방법으로 대신하거나 검토 항목으로 남김. CTU 안전 + 래싱 끔은 CTU 전도 기준으로 배치(평가 37건 74대), 에어백 간극 한계 500mm로 CTU 안전 56→64대. 최종 검사의 연쇄 제거 방지. 엔진 식별자 `ep-lex-portfolio-2026.10.13` |
| 3.20 | 2026-09-26 | CTU 안전: 벽 쌓기 배치안, 최종 검사 후 반복 재배치, 빈 컨테이너로 포기하지 않기. 샘플 3(크기 차이) 첫 컨테이너 13개 → 33개, 평가 세트 CTU 안전 64→60대. 엄격·많이 싣기 결과 변화 없음. 재배치 시 원래 미적재 사유(중량 초과 등) 유지. 엔진 식별자 `ep-lex-portfolio-2026.10.14` |
| 3.21 | 2026-09-26 | CTU 안전: 전폭 벽(스트립) 빌더 추가(Codex 자문 반영). 샘플 3 2대→1대, 평가 세트 CTU 안전(래싱 사용) 60→56대, 래싱 끔 81→77대. 이웃 화물 검사에 바닥 격자 색인(배치 결과 111건 동일 확인). 결과 요약(안전 판정·꼭 필요한 고정재·확인 항목)과 현장 결과 기록(계획 대비 비교, 프로젝트 저장) 추가. 엔진 식별자 `ep-lex-portfolio-2026.10.15` |
| 3.22 | 2026-09-26 | 결과 고정: 시간 대신 문제 크기로 정한 배치안 수만큼 계산(같은 평가 세트를 두 번 돌려 111건 모두 동일, 느린 컴퓨터를 흉내 낸 테스트 추가). 평가 세트 전체 계산 시간 145→86초, CTU 안전 56→54대, 엄격·많이 싣기 변화 없음. 엔진 식별자 `ep-lex-portfolio-2026.10.16` |
| 3.23 | 2026-09-26 | CTU 안전: 다른 크기 화물 위에 얹힌 화물은 문쪽 면이 막혀야 함(20개 샘플 점검에서 얹힘 7개→0개, 대수 +1). 평가 세트 CTU 안전(래싱 사용) 55대, 래싱 끔 66대(1.1.34 77대), 엄격·많이 싣기 변화 없음. 엔진 식별자 `ep-lex-portfolio-2026.10.17` |
