# Cubestow

```text
   ____      _               _
  / ___|   _| |__   ___  ___| |_ _____      __
 | |  | | | | '_ \ / _ \/ __| __/ _ \ \ /\ / /
 | |__| |_| | |_) |  __/\__ \ || (_) \ V  V /
  \____\__,_|_.__/ \___||___/\__\___/ \_/\_/

      Cubestow — Load wisely. Ship safely.
```

이전 이름: LoadWise

제품 규격, 중량, 수량과 적재 제약을 입력하면 컨테이너별 배치·적재 순서·고정재 위치를 계산하고 3D로 보여주는 브라우저 기반 컨테이너 로딩 시뮬레이터입니다.

- 현재 단계: Vercel 공개 베타 · 버전 1.1.56 (버전은 package.json의 semver로 관리하고 화면 하단에 같은 값을 표시한다. LoadWise 시절의 4.0.0까지는 이전 제품 버전이며, Cubestow로 이름을 바꾸면서 1.x로 다시 시작했다.)
- 서비스: https://cubestow.vercel.app/
- 저장소: https://github.com/cowpigpapa/cubestow
- 제품 요구사항: [LOADING-SIMULATOR-PRD.md](LOADING-SIMULATOR-PRD.md)
- 상단 `알고리즘 정책` 팝업 및 원문: [ALGORITHM-POLICY.md](ALGORITHM-POLICY.md)

## 주요 기능

- 직접 입력 및 Excel/CSV 업로드
- 박스형·원통형 제품 지원
- 등록 제품 수량 직접 입력 및 화살표 조절
- 눕힘 허용·상부 적재 금지 수정
- 20ft, 40ft, 40ft High Cube, 45ft High Cube 지원
- 안전 기준(엄격·표준) × 우선 기준(자동 추천·공간 활용·폭 균형·무게중심) 2축 선택
- Extreme Point 후보 + 아래·안쪽·좌측 밀착, 항목별 순서(사전식) 비교
- 배치 규칙 6종(같은 규격·균형 기둥 쌓기 포함) × 투입 순서 4종 결정론적 포트폴리오, Web Worker 백그라운드 계산
- 부피·중량 기반 컨테이너 대수 하한 표시
- 생성 엔진과 분리된 안전 validator 및 benchmark 회귀 게이트
- 지지율과 중심 지지 검사
- 해상·복합·육상 운송모드별 높은 적층·측면 간극·문측 노출·원통 구름 위험 선별
- 단일 컨테이너 부족 시 복수 컨테이너 자동 분할
- 안쪽부터 진행되는 단계별 적재 재생
- Three.js 기반 3D·상면·문 기준·좌측면·우측면 보기
- 전후·좌우 중량 배분, 무게중심 위치와 편차 판정
- CTU 중량배분 사전검사: 중심 편차, 수직 무게중심, 60-in-50 질량 집중도
- 제품별 상부 허용하중과 접촉면 기반 누적 압축하중 검증
- 문쪽 각재·부목 및 벽면·화물 간 에어백 추천
- 고정재 표시 토글
- 계산 진행률과 완료 시간
- 전체 계획 Excel 내보내기
- 새 창 미리보기와 인쇄·PDF 저장을 지원하는 작업지시서
- 실제 적재 수량·컨테이너 수·추가 고정재·특이사항 수기 기록란
- Supabase 기반 일일·누적 방문자 표시(브라우저당 하루 1회 기준)
- 이메일 기반 관리자 권한과 사용자별 저장·시뮬레이션·접속 통계
- 제품 목록과 컨테이너 설정을 프로젝트로 저장·불러오기
- 저장 프로젝트를 불러오면 3D 시뮬레이션 자동 실행
- Google OAuth·8자리 이메일 OTP 로그인과 계정별 클라우드 저장
- 저장 스키마·알고리즘 버전과 마지막 계산 결과 요약 기록

## 프로젝트 저장

첫 저장에서 이름을 확인하고, 이후 `저장`은 확인 후 현재 프로젝트를 갱신합니다. `신규 저장`은 현재 작업을 다른 이름의 새 프로젝트로 보존합니다. Excel/CSV 파일명이나 처음 등록한 제품명이 저장 이름으로 자동 제안되며, `저장 목록`에서 기존 프로젝트를 불러오거나 삭제할 수 있습니다. 저장 프로젝트를 불러오면 저장된 제품과 컨테이너 조건으로 시뮬레이션이 즉시 실행됩니다.

- 비로그인: 현재 브라우저 프로필의 `localStorage`에 저장
- Google OAuth와 8자리 이메일 OTP 로그인: Supabase의 사용자 계정별 클라우드 저장

### Microsoft 로그인 보류

- 운영 계정: `loadwise.app@outlook.com`
- 2026-08-13 기준 Microsoft 개인 계정만으로는 Entra 앱 신규 등록이 비활성화되어 있다.
- Azure 디렉터리 생성을 위해 무료 Azure 가입, 전화번호 및 결제카드 본인 확인이 필요해 설정을 중단했다.
- Entra 앱 등록과 Supabase Azure 공급자 연결이 완료될 때까지 Microsoft 로그인 버튼은 숨긴다.
- 상단 계정 버튼은 로그인 상태에 따라 `로그인`과 `로그아웃`으로 전환
- 로그인 상태에서는 이메일과 `클라우드` 저장 위치를 상단에 표시
- `새 프로젝트`는 제품·설정·시뮬레이션 결과를 초기 상태로 되돌림
- 저장하지 않은 변경사항이 있으면 프로젝트 교체 전에 확인
- 손상된 로컬 데이터는 자동으로 덮어쓰지 않음

로컬 저장은 다른 브라우저나 기기로 동기화되지 않으며 같은 브라우저 프로필을 사용하는 사람과 분리되지 않습니다. 개인별 보안과 여러 기기 연동이 필요하면 로그인해야 합니다. 클라우드 프로젝트는 PostgreSQL RLS 정책으로 로그인 사용자별 접근을 제한합니다.

최초 관리자는 데이터베이스 시드로 등록합니다. 관리자는 로그인 사용자별 현재 저장 프로젝트 수, 완료된 시뮬레이션 누계, 최초·최근 접속과 접속 횟수를 확인하고 다른 이메일에 관리자 권한을 부여할 수 있습니다. 비로그인 방문자는 이메일을 수집하지 않고 기존 오늘·누적 방문 수에만 포함됩니다.

## 입력 항목

| 항목 | 설명 |
|---|---|
| 제품명·제품군 | 제품 식별 정보 |
| 제품 형상 | 박스형 또는 원통형 |
| 길이·너비·높이 | mm 단위 외형 치수 |
| 개당 중량 | kg 단위 중량 |
| 수량 | 동일 규격 제품 수량 |
| 눕힘 허용 | 높이 축을 바꾸는 회전 허용 여부, 기본값은 미선택 |
| 상부 적재 금지 | 해당 제품 위에 다른 화물을 놓지 않음 |

바닥면에서 길이와 너비를 바꾸는 회전은 항상 평가합니다. `눕힘 허용`은 제품을 옆으로 눕혀 높이 축까지 바꿔도 되는지를 뜻합니다.

## 적재 기준

결과는 **안전 기준**과 **우선 기준** 두 가지로 정합니다. 안전 기준은 모든 배치가 반드시 지켜야 하는 조건이고, 우선 기준은 같은 안전 기준 안에서 미배치 수량과 컨테이너 대수가 같은 결과 중 무엇을 고를지 정합니다. 따라서 우선 기준을 바꿔도 컨테이너가 늘어나지 않습니다.

### 안전 기준

| 기준 | 상부 지지율 | 상부에 올릴 수 있는 화물 |
|---|---|---|
| 엄격 (기본) | 바닥면 100% | 높이 ÷ 바닥 최소 치수 1.15 이하 |
| 표준 | 바닥면 70% 이상 | 제한 없음 |

두 기준 모두 중심 지지, 상부 적재 금지, 원통 적층 조건, 상부 허용하중, 누적 높이가 바닥 최소 치수의 1.5배를 넘을 때 2방향 측면 지지를 요구합니다.

### 우선 기준

| 기준 | 대수가 같을 때 먼저 비교하는 항목 |
|---|---|
| 자동 추천 (기본) | CTU 사전검사 등급 → 운송 안정성 검토 건수 → 질량 집중·수직 CoG 초과 → 중심 편차 → 사용 길이 |
| 공간 활용 | 사용 길이 → CTU 등급 → 검토 건수 → 중심 편차 |
| 폭 균형 | 운송 안정성 검토 건수 → CTU 등급 → 사용 길이 → 중심 편차 |
| 무게중심 | CTU 등급 → 중심 편차 → 검토 건수 → 사용 길이 |

### 계산 방식

- 배치 규칙 6종(같은 규격 기둥 쌓기, 균형 기둥 쌓기, 바닥 우선 DBLF, 공간 집약, 폭 채움, 무게중심)과 투입 순서 4종(부피·최장변·바닥면·중량)을 모두 계산합니다.
- 컨테이너마다 적재 부피가 가장 큰 결과를 고르고, 같으면 우선 기준으로 고릅니다. 화물군을 안쪽 벽에 붙인 안과 무게중심 쪽으로 옮긴 안을 함께 비교합니다.
- 결과 요약에 부피·중량 하한 대수를 함께 표시해, 하한과 같으면 더 줄일 수 없다는 것을 알려 줍니다.
- 계산은 Web Worker에서 실행되어 화면이 멈추지 않습니다. 기본 시간 예산은 8초이며, 넘으면 남은 후보를 생략하고 결과에 표시합니다.

## 고정재 추천 기준

- 문쪽 노출 화물에는 폭 전체를 받치는 가로 각재와 부목 3~6개를 추천합니다.
- 좌측·우측·안쪽 벽 간극이 100mm 이상이면 바닥부터 에어백을 추천합니다.
- 600mm가 넘는 벽 간극은 600mm 이하의 여러 에어백으로 분할합니다.
- 화물 사이 간극은 120~600mm 범위에서 충돌이 없을 때 에어백을 추천합니다.
- 측면 지지는 각 방향에서 화물 폭 또는 높이의 50% 이상 맞닿을 때 인정합니다.
- 해상은 피칭·롤링·히빙에 불리한 높은 적층과 측면 지지를 가장 보수적으로 평가하고, 복합운송은 중간, 육상은 일부 완화한 기준을 사용합니다. 이 위험도는 결과 경고뿐 아니라 실제 후보 위치 선택에도 우선 반영됩니다.
- 자동 래싱 밴드 수량과 3D 경로는 생성하지 않습니다. 대신 불리한 배치에 `현장 고정 검토` 또는 `배치 재검토`와 구체적인 사유를 표시합니다.
- 원통 상부 적재는 바닥 지지와 중심 지지, 최소 2방향 측면 지지를 모두 만족하면 허용합니다.
- 실제 작업 전 컨테이너 바닥 고정 허용 여부와 고정재 제조사 지침을 확인해야 합니다.

## 좌표 체계

- X: 컨테이너 문에서 안쪽
- Y: 컨테이너 좌측에서 우측
- Z: 컨테이너 바닥에서 위

적재 순서는 기본적으로 가장 안쪽부터 문쪽으로, 같은 위치에서는 아래층부터 생성됩니다.

## 실행

정적 웹 애플리케이션이며 로컬 개발 서버와 배포용 빌드를 제공합니다.

```bash
npm test
npm run benchmark          # 회귀 게이트, 결과 파일은 바꾸지 않음
npm run benchmark:write    # 기준 결과(benchmarks/*.json) 갱신
npm run build
npm run test:e2e
```

주요 파일:

```text
index.html          화면 구조
styles.css          기본 스타일
layout-fixes.css    반응형 레이아웃 보정
app.js              입력, 화면, 3D, 내보내기
packing-engine.js   DOM과 분리된 적재 엔진
engine-worker.js    엔진을 백그라운드에서 실행하는 Web Worker
project-model.js    저장 데이터 정규화
project-store.js    로컬·Supabase 저장과 로그인
supabase-schema.sql 사용자별 저장 테이블과 RLS 정책
vendor/three.min.js 로컬 3D 엔진
LOADING-SIMULATOR-PRD.md 제품 요구사항 문서
ALGORITHM-POLICY.md 알고리즘 원칙·판정 기준·한계 문서
ALGORITHM-LEARNING.md 전략 경쟁·현장 피드백·학습 승격 정책
SIMULATION-FEEDBACK.md 시뮬레이션 질문·판정·개선 이력
test-projects/      반복 검토용 CSV 화물 10종
solution-validator.js 독립 결과 검증기
benchmark.mjs       안전 기준 × 우선 기준별 품질·안전·시간 회귀 게이트
scenario-benchmark.mjs  10개 CSV 회귀 검사
```

## 기술 구성

- HTML, CSS, Vanilla JavaScript
- Three.js WebGL 렌더링
- SheetJS Excel 입출력
- Supabase Auth·Postgres(계정별 저장)
- Vercel 자동 배포

## 한계

현재 엔진은 휴리스틱으로 좋은 배치를 빠르게 찾지만 전역 최적해를 보장하지 않습니다. 상부 허용하중이 입력된 제품만 누적 압축하중을 검사하며, 실제 출하 전에는 미입력 제품의 압축강도와 면압, 바닥 허용 하중, 축하중, 운송 진동, 고정재 사양과 관련 안전 규정을 별도로 검토해야 합니다.
